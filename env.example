# ============================================
# 环境变量配置示例文件
# ============================================
# 
# 使用说明：
# 1. 复制此文件为 .env.local: cp .env.example .env.local
# 2. 填写实际的环境变量值
# 3. .env.local 已添加到 .gitignore，不会被提交到仓库
# 4. 生产环境（Vercel）：在 Vercel Dashboard > Settings > Environment Variables 中配置
#
# 注意：
# - 所有服务端环境变量只能在 API 路由、Server Components 中使用
# - 客户端环境变量必须以 NEXT_PUBLIC_ 开头
# - 不要在客户端暴露敏感信息（如 API Secret）
#
# ============================================

# ============================================
# Hasura GraphQL 配置
# ============================================
# GraphQL API 端点地址（如果需要在 goc.config.ts 中使用环境变量，可取消注释）
# HASURA_ENDPOINT=https://your-hasura-instance.com/v1/graphql

# Hasura Admin Secret（如果需要在 goc.config.ts 中使用环境变量，可取消注释）
# HASURA_ADMIN_SECRET=your-admin-secret

# Hasura JWT Secret（用于生成 JWT Token，服务端使用）
# 默认值: hasura_graphql_jwt_secret_key_for_HS256
# 配置文件: src/config-lib/hasura/config.ts
HASURA_JWT_SECRET=your-jwt-secret-key-here

# ============================================
# 阿里云短信服务配置（服务端）
# ============================================
# 配置文件: src/config-lib/ali/config.ts
ALI_SMS_ACCESS_KEY_ID=your-ali-sms-access-key-id
ALI_SMS_ACCESS_KEY_SECRET=your-ali-sms-access-key-secret

# ============================================
# 七牛云存储配置（服务端）
# ============================================
# 配置文件: src/config-lib/qiniu/config.ts
QINIU_ACCESS_KEY=your-qiniu-access-key
QINIU_SECRET_KEY=your-qiniu-secret-key
QINIU_BUCKET=your-bucket-name

# 以下为可选配置
# CDN 域名，用于拼接文件访问 URL
QINIU_BASE_URL=https://your-cdn-domain.com
# 存储目录路径，自动生成 key 时使用
QINIU_DIR_PATH=uploads/

# ============================================
# 微信小程序配置（服务端）
# ============================================
# 配置文件: src/config-lib/weixin/config.ts
WX_APP_ID=your-wechat-app-id
WX_APP_SECRET=your-wechat-app-secret

# ============================================
# 微信支付配置（服务端）
# ============================================
# 配置文件: src/config-lib/weixin/config.ts
# 商户号
WX_PAY_MCHID=your-merchant-id
# 支付 AppID
WX_PAY_APPID=your-wechat-pay-appid
# 证书序列号
WX_PAY_SERIAL=your-certificate-serial-number
# 私钥（通常是一个多行的 PEM 格式字符串，在环境变量中使用 \n 表示换行）
WX_PAY_PRIVATE_KEY=your-private-key-here
# APIv3 密钥
WX_PAY_APIV3_KEY=your-apiv3-key-here
# 支付回调地址
WX_PAY_NOTIFY_URL=https://your-domain.com/api/weixin/pay/notify

# ============================================
# 客户端环境变量（可选，必须以 NEXT_PUBLIC_ 开头）
# ============================================
# 如果需要在客户端访问 GraphQL endpoint（不推荐，除非是公开的 API）
# NEXT_PUBLIC_HASURA_ENDPOINT=https://your-hasura-instance.com/v1/graphql

# 其他客户端环境变量示例
# NEXT_PUBLIC_API_URL=https://api.your-domain.com
# NEXT_PUBLIC_APP_NAME=My App
# NEXT_PUBLIC_APP_VERSION=1.0.0